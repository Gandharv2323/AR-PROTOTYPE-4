# ARVTON Local Development Environment
# Copy from .env.example and adjust as needed

# ── Server ──
HOST=0.0.0.0
PORT=8000
ARVTON_DEV=1

# ── CORS (allow everything for local dev) ──
ALLOWED_ORIGINS=*

# ── Storage ──
STORAGE_BACKEND=local
ARVTON_OUTPUT_DIR=outputs

# ── Base URL (used for GLB download links) ──
BASE_URL=http://localhost:8000

# ── GPU (leave empty to auto-detect) ──
# CUDA_VISIBLE_DEVICES=0
