name: arvton_app
description: ARVTON — AR/VR Virtual Try-On Flutter Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

  # Navigation
  go_router: ^13.2.0

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Networking
  dio: ^5.4.3

  # Camera & Media
  camera: ^0.10.5+9
  image_picker: ^1.0.7

  # AR & 3D
  ar_flutter_plugin: ^0.7.3
  model_viewer_plus: ^1.7.0

  # UI & Animations
  lottie: ^3.1.0
  cached_network_image: ^3.3.1

  # Sharing
  share_plus: ^9.0.0

  # Environment
  flutter_dotenv: ^5.1.0

  # Utils
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.3
  permission_handler: ^10.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/animations/
    - .env
